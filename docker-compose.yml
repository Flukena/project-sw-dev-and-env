version: '3.8'

services:
  # backend: 
  #   build:
  #     context: ./project_dev-tools_server
  #     target: back
  #     dockerfile: ./Dockerfile

  #   command: npm run serve

  frontend:
    build:
      context: ../project_dev-tools_frontend/dist
      target: front
      dockerfile: ./Dockerfile
    command: npm start

